#include <stdio.h>
#include <stdbool.h>

void factorial(int }) {
    if ((n <= 1))     {
        return 1;
    }
    return (n * factorial((n - 1)));
}

int main() {
    int result = factorial(5);
    printf("Factorial of 5 is %d", result);
    return 0;
    return 0;
}

