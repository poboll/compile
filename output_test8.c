#include <stdio.h>
#include <stdbool.h>

void add(int }, int }) {
    return (a + b);
}

void multiply(int }, int }) {
    return (a * b);
}

int main() {
    int x = add(3, 4);
    int y = multiply(x, 2);
    return y;
    return 0;
}

